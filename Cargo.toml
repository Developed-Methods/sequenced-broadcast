[package]
name = "sequenced-broadcast"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.40", features = ["time", "sync", "rt"] }
tokio-util = "0.7.12"
tracing = "0.1.40"

[dev-dependencies]
tracing-subscriber = "0.3.18"
tokio = { version = "1.40", features = ["full"] }
